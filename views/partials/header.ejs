<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MyShow</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/main.css">
</head>


<body data-bs-theme="dark">
    <!--navbar with main page, shows, lists and login/logout routes-->
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">MyShow</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a class="nav-link" href="/show">Shows</a>
              </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">My Lists</a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="/myShowList">My Shows</a></li>
                    </ul>   
                </li>
            </ul>
            <!--checking if the user is logged in-->
            <% if (locals.currentUser && currentUser[0] != 'nope') { %>
                <ul class="navbar-nav px-5 mb-2 mb-lg-0">
                  <li class="nav-item dropdown">
                      <a class="nav-link dropdown-toggle right" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Hi, <%= currentUser[0] %></a>
                      <ul class="dropdown-menu">
                          <li><a class="dropdown-item" href="/logout">Log out</a></li>
                      </ul>   
                  </li>
              </ul>
            <% } else {%>
                <a class="nav-link px-5 right" href="/">Log in</a>
            <% } %>
          </div>
        </div>
      </nav>
